# Personal portfolio, Krupesh Joshi

A single-page personal site: hero introduction, two project case studies (Reconcile and Untangle), work experience, open-source contributions, and contact links.

**Live demo:** _add your deployed URL here_

## Running it locally

```bash
npm install
npm run dev
```

## Before you deploy

- Update the `demoHref` and `codeHref` values in `src/App.tsx` for both case studies once Reconcile and Untangle are actually deployed, they currently point to `#`.
- Every line of experience and project copy on this page is written to match only what's documented and verified elsewhere, if anything about a role or project changes, update it here too rather than letting this page drift out of sync.

## Tech stack

React, TypeScript, Vite, Tailwind CSS v4. Fonts: Space Grotesk, Inter, IBM Plex Mono.
